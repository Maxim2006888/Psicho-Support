<UserControl x:Class="Psicho_Support.Views.Pages.TestsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:Psicho_Support.Helpers"
             Background="{DynamicResource BackgroundBrush}">

    <UserControl.Resources>
        <helpers:BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="20">

        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        
        <StackPanel Grid.Row="0" Margin="0,0,0,20">

            <TextBlock Text="Психологические тесты"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="{DynamicResource AccentBrush}"/>

            <TextBlock Text="Пройдите тесты для лучшего понимания своего состояния"
                       FontSize="14"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       Margin="0,4,0,16"/>

            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Padding="10"
                     Background="{DynamicResource SurfaceBrush}"
                     BorderBrush="{DynamicResource BorderBrush}"
                     BorderThickness="1"/>
        </StackPanel>

        <!-- ===== СПИСОК ТЕСТОВ ===== -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">

            <ItemsControl ItemsSource="{Binding AvailableTests}">

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>

                        <Border Width="260"
                                CornerRadius="12"
                                Background="{DynamicResource SurfaceBrush}"
                                Padding="16"
                                Margin="8">

                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10" Opacity="0.2"/>
                            </Border.Effect>

                            <StackPanel>

                                <TextBlock Text="{Binding TestName}"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           TextWrapping="Wrap"
                                           Margin="0,0,0,8"/>

                                <TextBlock Text="{Binding Description}"
                                           FontSize="13"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           TextWrapping="Wrap"
                                           Margin="0,0,0,12"/>

                                <Button Content="Начать"
                                        Padding="8"
                                        Background="{DynamicResource AccentBrush}"
                                        Foreground="White"
                                        Command="{Binding DataContext.StartTestCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </Border>

                    </DataTemplate>
                </ItemsControl.ItemTemplate>

            </ItemsControl>
        </ScrollViewer>

        <!-- ===== ЗАТЕМНЕНИЕ (ТЕСТ) ===== -->
        <Border Grid.RowSpan="2"
                Background="#80000000"
                Visibility="{Binding IsTestStarted, Converter={StaticResource BoolToVis}}"/>

        <!-- ===== ОКНО ТЕСТА ===== -->
        <Border Grid.RowSpan="2"
                Width="360"
                Height="420"
                Background="{DynamicResource BackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="12"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Visibility="{Binding IsTestStarted, Converter={StaticResource BoolToVis}}">

            <Grid Margin="20">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Название теста -->
                <TextBlock Grid.Row="0"
                           Text="{Binding SelectedTest.TestName}"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="{DynamicResource AccentBrush}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,10"/>

                <!-- Прогресс -->
                <TextBlock Grid.Row="1"
                           Text="{Binding CurrentQuestionNumber}"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           Margin="0,0,0,10"/>

                <!-- Вопрос -->
                <Border Grid.Row="2"
                        CornerRadius="10"
                        Background="{DynamicResource SurfaceBrush}"
                        Padding="15"
                        Margin="0,0,0,10">

                    <TextBlock Text="{Binding CurrentQuestion.Text}"
                               Foreground="White"
                               TextWrapping="Wrap"/>
                </Border>

                <!-- Ответы -->
                <ScrollViewer Grid.Row="3"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">

                    <ItemsControl ItemsSource="{Binding CurrentQuestion.Answers}">

                        <ItemsControl.ItemTemplate>
                            <DataTemplate>

                                <Button Margin="0,4"
                                        Padding="12"
                                        HorizontalAlignment="Stretch"
                                        HorizontalContentAlignment="Left"
                                        Command="{Binding DataContext.SubmitAnswerCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}">

                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                            <Setter Property="Foreground" Value="White"/>

                                            <Style.Triggers>

                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#2AFFFFFF"/>
                                                </Trigger>

                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Trigger>

                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>

                                    <TextBlock Text="{Binding Text}" 
                                               Foreground="White"
                                               TextWrapping="Wrap"/>
                                </Button>

                            </DataTemplate>
                        </ItemsControl.ItemTemplate>

                    </ItemsControl>

                </ScrollViewer>

                <!-- Кнопка отмены -->
                <Button Grid.Row="4"
                        Content="Отмена"
                        Command="{Binding ResetTestCommand}"
                        Background="Transparent"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        Foreground="White"
                        Padding="10,5"
                        HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <!-- ===== ЗАТЕМНЕНИЕ (РЕЗУЛЬТАТ) ===== -->
        <Border Grid.RowSpan="2"
                Background="#80000000"
                Visibility="{Binding IsTestCompleted, Converter={StaticResource BoolToVis}}"/>

        <!-- ===== ОКНО РЕЗУЛЬТАТА ===== -->
        <Border Grid.RowSpan="2"
                Width="360"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="12"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Visibility="{Binding IsTestCompleted, Converter={StaticResource BoolToVis}}">

            <StackPanel Margin="30">

                <TextBlock Text="Результат"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="{DynamicResource AccentBrush}"
                           TextAlignment="Center"
                           Margin="0,0,0,20"/>

                <TextBlock Text="{Binding TotalScore, StringFormat='Ваш результат: {0}'}"
                           FontSize="18"
                           TextAlignment="Center"
                           Foreground="White"
                           Margin="0,0,0,15"/>

                <Border Background="{DynamicResource BackgroundBrush}"
                        Padding="15"
                        CornerRadius="10">

                    <TextBlock Text="{Binding TestResultMessage}"
                               Foreground="White"
                               TextWrapping="Wrap"
                               TextAlignment="Center"/>
                </Border>

                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Margin="0,20,0,0">

                    <Button Content="К тестам"
                            Command="{Binding ResetTestCommand}"
                            Margin="5"/>

                    <Button Content="Повторить"
                            Command="{Binding StartTestCommand}"
                            Margin="5"/>
                </StackPanel>

            </StackPanel>
        </Border>

    </Grid>
</UserControl>